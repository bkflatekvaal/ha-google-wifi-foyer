{
  "title": "Google Wifi Foyer",
  "config": {
    "step": {
      "user": {
        "title": "Connect to Google Wifi",
        "description": "Open https://accounts.google.com/EmbeddedSetup in a browser, sign in to the Google account that owns or manages the Wifi network, then copy the full `oauth_token` cookie value (including `oauth2_4/`). The cookie is only used during setup and is not stored.",
        "data": {
          "email": "Google account email",
          "oauth_token": "EmbeddedSetup oauth_token"
        },
        "data_description": {
          "email": "The Google account that has access to the Wifi network.",
          "oauth_token": "The complete value of the `oauth_token` cookie from accounts.google.com."
        }
      },
      "network": {
        "title": "Choose Wifi network",
        "description": "Choose the Google Wifi network to add. Run the integration setup again if you want to add another network from the same account.",
        "data": {
          "group_id": "Wifi network"
        }
      },
      "reauth_confirm": {
        "title": "Reconnect Google Wifi",
        "description": "Authentication for {email} needs to be renewed. Get a fresh `oauth_token` from https://accounts.google.com/EmbeddedSetup.",
        "data": {
          "oauth_token": "EmbeddedSetup oauth_token"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to the Google Wifi Foyer service.",
      "invalid_auth": "Google authentication failed. Get a fresh oauth_token from EmbeddedSetup and try again.",
      "no_networks": "No Google Wifi networks were returned for this account.",
      "unknown": "Unexpected error."
    },
    "abort": {
      "already_configured": "This Google Wifi network is already configured.",
      "reauth_required": "Authentication needs to be started again.",
      "reauth_successful": "Google Wifi authentication was renewed successfully."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Guest network credentials",
        "description": "Optional fallback used when Foyer cannot retrieve the guest password while the network is disabled. Enter both fields or leave both empty. The password is stored in Home Assistant's config entry and is included in backups.",
        "data": {
          "guest_ssid": "Guest network name (SSID)",
          "guest_psk": "Guest network password"
        },
        "data_description": {
          "guest_ssid": "The existing guest network name used by Google Home.",
          "guest_psk": "The existing guest network password. It is not exposed as an entity state or attribute."
        }
      }
    },
    "error": {
      "guest_credentials_incomplete": "Enter both the guest SSID and password, or leave both empty."
    }
  }
}
